#include <stdio.h>
#include <string.h>

void set_string(char **s, char *to); {
    strcpy(point, "asd");
}

int main() {
    char point[10];
    ptrch(point);
    printf("%s\n", point);
    return 0;
}
